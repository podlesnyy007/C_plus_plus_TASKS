#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

float f(float x, float e, int &n) {
    float s = 0, a = 1;  // начальное значение суммы и первое слагаемое
    n = 0;  // количество просуммированных слагаемых
    while (fabs(a) >= e) {
        s += a;
        a *= 
    }
}

int main() 
{
    
}
